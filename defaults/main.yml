---

# default docker network
docker_network_subnet: '172.18.0.0/16'
docker_network_gateway: '172.18.0.1'
docker_network_name: "custom-bridge"
docker_network_device_name: br-custom0

docker_networks:
  - "{{ docker_network_name }}"
